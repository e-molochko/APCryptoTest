import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    coinItem: {
        flexDirection: 'row',
        padding: 10,
        alignItems: 'center',
    },
    coinSymbol: {
        alignItems: 'center',
        justifyContent: 'center',
        marginRight: 20,
        padding: 5,
        borderWidth: 0.5,
    },
    symbolIcon: { fontWeight: 'bold' },
})
